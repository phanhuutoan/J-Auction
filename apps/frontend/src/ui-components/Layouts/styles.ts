import { defineStyle } from "@chakra-ui/react";

export const layoutStyle = defineStyle({
  root: {
    alignItems: "center",
    justifyContent: "space-between",
    padding: "1rem 2.5rem",
    bgColor: "purple.500",
    boxShadow: "md",
    color: "white",
  },
});

import { defineStyle } from "@chakra-ui/react";

export const loginStyle = defineStyle({
  root: {
    maxWidth: "40rem",
    padding: 8,
    shadow: "md",
    borderRadius: ".5rem",
    bgColor: "white",
    margin: "0 auto",
    marginTop: "2rem",
  },
});
